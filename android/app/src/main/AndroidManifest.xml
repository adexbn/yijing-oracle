<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".YijingApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Yijing"
        android:usesCleartextTraffic="true">

        <!--
            OpenCL 后端（MNN 的 libMNN_CL.so）靠 dlopen 在运行时加载系统的 libOpenCL.so，
            这不是硬依赖（已用 llvm-readelf -d 逐个核对过 jniLibs 里 9 个 .so，没有任何一个
            NEEDED libOpenCL.so），所以：
              * required="false" —— 没有 OpenCL 驱动的机器照样能装、能跑（自动退回 CPU）；
              * Android 12+ 起应用默认访问不到非 NDK 的厂商库，必须在这里显式申请，
                否则高通 Adreno 机器上 dlopen 会被拦掉，白白退回 CPU。

            位置：必须挂在 <application> 下面（与 <uses-library> 同级），不能放 <manifest> 下。
            这一点是实测出来的：用本机 build-tools 的 aapt2 单独跑过两种写法，
            放 <manifest> 下会 `unexpected element <uses-native-library> found in <manifest>`
            直接链接失败；放 <application> 下才通过。MNN 官方 MnnLlmChat 也是这么写的。
        -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 首次使用初始化页：下载模型耗时长，配置变化（旋转）不要重建，否则下载会被打断。 -->
        <activity
            android:name=".OnboardingActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|screenLayout"
            android:exported="false" />

        <activity
            android:name=".ResultActivity"
            android:exported="false" />

        <activity
            android:name=".LoadingActivity"
            android:exported="false" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false" />

        <activity
            android:name=".SettingsActivity"
            android:exported="false" />

        <!-- 设置页「日志与诊断」里把性能日志文件分享出去用 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>